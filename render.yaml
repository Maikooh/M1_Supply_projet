services:
  - type: web
    name: m1-supply-dashboard
    runtime: python
    plan: free
    buildCommand: uv sync --frozen
    startCommand: uv run gunicorn dashboard:server
    envVars:
      - key: PYTHON_VERSION
        value: 3.13.13